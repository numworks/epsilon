#include <quiz.h>
#include <poincare.h>
#include <assert.h>

QUIZ_CASE(poincare_multiply_approximate) {
  //Context context;
  //Integer a(1), b(2);
  //assert(Product(&a, &b).approximate(context) == 2);
}
