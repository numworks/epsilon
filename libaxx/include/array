#ifndef LIBAXX_ARRAY
#define LIBAXX_ARRAY

#include <stddef.h>

namespace std {

template <class T, size_t N>
constexpr size_t size(const T (&array)[N]) noexcept {
  return N;
}

}  // namespace std

#endif
